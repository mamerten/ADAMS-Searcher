{
  "name": "adams-web-searcher",
  "version": "0.3.1",
  "private": true,
  "scripts": {
    "sync-skill": "node gen-skill.mjs",
    "sync-refs": "node gen-references.mjs",
    "sync-rmp-refs": "node gen-rmp-reference.mjs",
    "sync": "npm run sync-skill && npm run sync-refs && npm run sync-rmp-refs",
    "predev": "npm run sync",
    "dev": "wrangler pages dev",
    "deploy": "wrangler pages deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
